# .env.example
# ==========================================
# Frontend Secrets (Vite Environment)
# ==========================================
# To hide Firebase configuration from the public JSON file, copy these variables
# into your hosting service's secrets or a local .env file.
VITE_FIREBASE_PROJECT_ID=your_proj_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain
VITE_FIRESTORE_DATABASE_ID=your_db_id
VITE_FIREBASE_STORAGE_BUCKET=your_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id

VITE_GEMINI_API_KEY=your_gemini_api_key
VITE_ADMIN_EMAIL=moashrafsy@gmail.com

# ==========================================
# Backend Secrets (Server Environment)
# ==========================================
# Used to derive Vault cryptography keys 
ENCRYPT_KEY=your_very_secure_random_string_here
VAULT_SALT=another_random_salt_string

# Google Analytics 4
VITE_GA_ID=G-XXXXXXXXXX

# Firebase Admin Credentials (optional, for backend saving of contacts/newsletters)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ==========================================
# Personal Data Scan (optional)
# ==========================================
# If provided, the /api/personal/scan endpoint will query the Have I Been Pwned
# v3 API for real breach data. When omitted, the endpoint returns a deterministic
# indicative assessment so the feature still works end-to-end without a key.
HIBP_API_KEY=

# ==========================================
# Replit / Hosting
# ==========================================
# The Express server binds to 0.0.0.0 and reads PORT from the environment.
# Replit injects PORT automatically; locally it falls back to 3000.
PORT=3000
NODE_ENV=development
